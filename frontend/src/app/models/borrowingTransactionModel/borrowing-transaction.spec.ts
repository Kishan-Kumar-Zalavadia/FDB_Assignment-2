import { BorrowingTransaction } from './borrowing-transaction';

describe('BorrowingTransaction', () => {
  it('should create an instance', () => {
    expect(new BorrowingTransaction()).toBeTruthy();
  });
});
